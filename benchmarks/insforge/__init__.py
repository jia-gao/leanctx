"""InsForge Model Gateway measurement harness (I3).

Drives InsForge's chat path with compression off vs on — LongBench v2 for
accuracy, a coding-agent transcript for savings — reporting InsForge's own
``usage.prompt_tokens`` and dollars via OpenRouter pricing. Reuses the
provider-agnostic core in ``benchmarks.common``.
"""
